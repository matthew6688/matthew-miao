# Cloudflare cutover record — 2026-08-09

## Release target

- Source repository: `matthew6688/matthew-miao`
- Production domain: `https://matthew-miao.com`
- Production Worker: `matthew-miao`
- Production cache: R2 `matthew-miao-next-cache`
- Persistent staging Worker: `matthew-miao-staging`
- Preview Worker: `matthew-miao-preview`
- Non-production cache: R2 `matthew-miao-staging-next-cache`

The apex and `www` hostnames are Cloudflare custom domains. `www` redirects to
the apex. Chinese is the default locale and English is served under `/en`.

## Verified release gates

- TypeScript typecheck
- 1,213 unit tests across 131 files
- bilingual content/localization checks
- deployment workflow and migration-safety checks
- Cloudflare/OpenNext production build
- hosted Chromium journeys and public discovery routes
- production and complete dependency audits with zero known vulnerabilities

## Rollback

Use Cloudflare Workers version rollback for an immediate runtime rollback. For
a source rollback, revert the release commit with a new Git commit, rebuild with
the production `SITE_URL`, and deploy through the Production workflow. Never
rewrite `main` history or delete the R2 cache bucket during rollback.

## Outstanding operator configuration

Add `CLOUDFLARE_API_TOKEN` to the GitHub repository/environment secrets to turn
validated CI runs into automatic deployments. Provider-backed Admin, AMA,
calendar, email, payment, and media features remain fail-closed until their
accounts and secrets are explicitly supplied and accepted.
